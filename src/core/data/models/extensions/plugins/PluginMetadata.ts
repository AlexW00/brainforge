import { ExtensionCategory } from "../ExtensionCategory";
import { ExtensionMetadata } from "../ExtensionMetadata";

/**
 * Represents the metadata of a plugin.
 */
export type PluginMetadata = ExtensionMetadata<ExtensionCategory.Plugin>;
