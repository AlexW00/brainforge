import { ExtensionCategory } from "../ExtensionCategory";
import { ExtensionMetadata } from "../ExtensionMetadata";

/**
 * Represents the metadata of a theme.
 */
export type ThemeMetadata = ExtensionMetadata<ExtensionCategory.Theme>;
